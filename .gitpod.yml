image:
  file: .gitpod.Dockerfile

tasks:
  - name: Setup & Run
    init: mvn clean install -DskipTests
    command: mvn spring-boot:run

ports:
  - port: 8080
    onOpen: open-browser
    visibility: public
